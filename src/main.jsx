import React from 'react'
import ReactDOM from 'react-dom/client'
import { HashRouter } from 'react-router-dom'
import App from './App.jsx'
import './index.css'

// HashRouter statt BrowserRouter: GitHub Pages kann keine Server-Rewrites,
// mit Hash-Routing funktioniert /#/admin ohne 404 beim Direktaufruf.
ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <HashRouter>
      <App />
    </HashRouter>
  </React.StrictMode>,
)
